// creates a tournament entrant with a mediaNode
const createEntrant = (mediaNode) => {
    const entry = {...mediaNode};
    console.log(entry);
    return entry;
}

const initializeTournament = () => {

}